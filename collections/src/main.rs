fn main() {
    // Vector

    // let v_empty: Vec<i32> = Vec::new();
    let v = !vec![1, 2, 3, 4, 5];
    let mut v_mutable: Vec<i32> = Vec::new();
    v_mutable.push(5);
    v_mutable.push(6);
    v_mutable.push(7);
    v_mutable.push(8);

    // Strings

    // Hash Maps
}
